#
# Generated by ansible role querybook_provision — DO NOT EDIT BY HAND.
#
# Source: ansible/group_vars/all/secrets.yml
#
# This file holds:
#   1. The QUERYBOOK_CONNECTIONS_FILE pointer so querybook reads
#      connections.yml on startup.
#   2. Plain KEY=value secrets used as ${VAR} substitutions inside
#      connections.yml (passwords, tokens, etc).
#
# Wire this file into your deployment:
#   docker-compose:  env_file: {{ querybook_env_file_path }}
#   systemd:         EnvironmentFile={{ querybook_env_file_path }}
#   kubectl:         kubectl create secret generic querybook-env-config \
#                        --from-env-file={{ querybook_env_file_path }}
#

QUERYBOOK_CONNECTIONS_FILE={{ querybook_connections_file_path }}

{% if querybook_env_extra_secrets is defined and querybook_env_extra_secrets %}
# --- Secret env vars (referenced via ${VAR} from connections.yml) ---
{% for key, value in querybook_env_extra_secrets.items() %}
{{ key }}={{ value }}
{% endfor %}
{% endif %}
